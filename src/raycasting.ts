
import * as Three from "three";

const scene = new Three.Scene();
const raycaster = new Three.Raycaster();

export const checkView = (triangles: Float32Array[]) => {
  // Loop through the meshes
}


